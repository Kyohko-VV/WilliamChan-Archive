---
import MusicWorkDetail from "../../components/works/MusicWorkDetail.astro";
import { works } from "../../data/works";

const work = works.find((entry) => entry.id === "music-yi-bi-jiang-hu");
if (!work) throw new Error("Music work data not found: music-yi-bi-jiang-hu");
---

<MusicWorkDetail work={work} />
