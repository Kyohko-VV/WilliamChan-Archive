---
import MusicWorkDetail from "../../components/works/MusicWorkDetail.astro";
import { works } from "../../data/works";

const work = works.find((entry) => entry.id === "music-yin-ni");
if (!work) throw new Error("Music work data not found: music-yin-ni");
---

<MusicWorkDetail work={work} />
