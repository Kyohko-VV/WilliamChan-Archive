---
interface Props {
  id: string;
  title: string;
  englishLabel?: string;
}

const { id, title, englishLabel } = Astro.props;
---

<section aria-labelledby={id}>
  <h2 id={id}>
    {title}
    {englishLabel && <span class="english-label"> / {englishLabel}</span>}
  </h2>
  <slot />
</section>
